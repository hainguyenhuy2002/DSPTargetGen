# Copy this file to `.env` and fill in your real values.
# The `.env` file is gitignored; `.env.example` is the committed template.

# --- Together AI ---------------------------------------------------------
# Get one from https://api.together.xyz/settings/api-keys
TOGETHER_API_KEY=your_together_api_key_here

# Optional — override the default backend or model without editing config.py.
# LLM_BACKEND=together
# TOGETHER_MODEL=deepseek-ai/DeepSeek-V3
